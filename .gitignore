# Credentials / local environment — never commit
.Renviron
.env
*.pgpass

# R session artifacts
.Rhistory
.RData
.Rproj.user/

# Quarto/Rmd preview output
*.html
*_files/
# ...but the Shiny UI template is source, not preview output.
!app/templates/*.html

# Logs
*.log

# Scratch / temp output (never commit)
tmp/
